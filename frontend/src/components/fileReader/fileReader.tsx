import React from 'react';
import styles from './fileReader.module.css';

const fileReader: React.FC = () => (
  <div className={styles.fileReader} data-testid="fileReader">
    fileReader Component
  </div>
);

export default fileReader;
